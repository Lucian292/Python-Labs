# Input text from the user
input_text = input("Enter a string: ")

# Convert uppercase letters to lowercase and replace spaces with underscores
output_text = input_text.lower().replace(" ", "_")

# Print the resulting string
print(output_text)
